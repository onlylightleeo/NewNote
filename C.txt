__attribute__  在任何情况，都会生成弱函数，无论是用于函数定义还是用于函数声明中
__weak  仅在函数定义使用才会生成弱函数

__inline 内联函数  放在函数前，把该函数的代码副本放在每个调用该函数的地方。
一般与static一起使用。
空间代价换时间代价。
内敛函数一般只有10行甚至更少的时候才将其定义为内联函数
在内联函数一般不允许使用循环语句和开关语句
有些函数即便声明为内联函数也不一定会被编译器内联，比如虚函数和提柜函数。